BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TBL_CORE_USERS] ALTER COLUMN [refreshToken] VARCHAR(200) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
